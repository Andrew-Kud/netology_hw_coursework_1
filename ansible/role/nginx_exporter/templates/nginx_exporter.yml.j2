listen:
  port: {{ nginx_exporter_port }}
  address: 0.0.0.0

namespaces:
  - name: nginx_access
    format: combined
    files:
      - /var/log/nginx/access.log

  - name: nginx_error
    format: error
    files:
      - /var/log/nginx/error.log
